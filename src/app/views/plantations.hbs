{{>nav}}
<link rel="stylesheet" href="/public/css/plantations.css">
<div class="container">
  <ul class="plantations-list">
    <a id="add" href="create-plantation">+</a>
    {{plantations}}
    {{#each plantations}}
    <li>
      <a href="plantation/{{name}}">
        <img src="{{logo.url}}" alt="{{name}}">
        <div class="name {{status}}">{{name}}</div>
        <span>ENTER</span>
      </a>
    </li>
    {{/each}}
  </ul>
</div>
